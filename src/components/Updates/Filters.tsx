import React from 'react';
import FilterBtn from "./FilterBtn";

const Filters = () => {
    return (
        <div className="mt-10">
            <FilterBtn key="1"/>
        </div>
    );
};

export default Filters;