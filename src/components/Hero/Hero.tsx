import React from 'react';
import HeroText from "./HeroText";

const Hero = () => {
    return (
        <div className="flex flex-col items-center justify-center h-96 py-3">
            <HeroText/>
        </div>
    );
};

export default Hero;
